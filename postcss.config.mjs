// FlowPilot v1.0.0 — PostCSS config for Tailwind CSS v4.
// Tailwind v4 uses CSS-first configuration (defined in globals.css @theme).
// Do NOT add a tailwind.config.ts — this project does not use one.

const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}

export default config
